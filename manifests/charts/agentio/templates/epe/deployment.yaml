{{- if eq .Values.epe.mode "managed" }}
{{- include "agentio.validate" . }}
{{- $source := .Values.epe.credentialProvider.mtls.source }}
{{- $certificateSource := .Values.epe.tls.certificateSource | default dict }}
{{- $ca := $certificateSource.ca | default dict }}
{{- $caMode := and .Values.epe.tls.enabled (not (hasKey $certificateSource "file")) }}
{{- $mountRoot := and $caMode (not (hasKey $ca "caCertificateFile")) }}
{{- $mountToken := and $caMode (not (hasKey $ca "tokenFile")) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "epe.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "epe.labels" . | nindent 4 }}
spec:
  {{- if not .Values.epe.autoscaling.enabled }}
  replicas: {{ .Values.epe.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "epe.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "epe.selectorLabels" . | nindent 8 }}
        agentio.kruise.io/dataplane-mode: none
        {{- with .Values.epe.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        {{- with .Values.epe.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "epe.serviceAccountName" . }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: epe
          image: {{ include "epe.image" . | quote }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          args:
            - -epe-config={{ include "epe.fullname" . }}-config
            - -epe-config-primary={{ include "epe.fullname" . }}-config-primary
            - -epe-config-namespace={{ .Release.Namespace }}
            {{- if .Values.epe.tls.enabled }}
            {{- if $caMode }}
            - -tls-source=ca
            - {{ printf "-ca-address=%s" (default (include "agentiod.caAddress" .) $ca.address) | quote }}
            - {{ printf "-ca-token-path=%s" (default "/var/run/secrets/tokens/agentio-token" $ca.tokenFile) | quote }}
            - {{ printf "-ca-root-path=%s" (default "/var/run/secrets/agentio/root-cert.pem" $ca.caCertificateFile) | quote }}
            - -tls-spiffe-id={{ printf "spiffe://%s/ns/%s/sa/%s" .Values.global.trustDomain .Release.Namespace (include "epe.serviceAccountName" .) }}
            {{- else }}
            - -tls-source=file
            - {{ printf "-tls-cert-path=%s" $certificateSource.file.certificateFile | quote }}
            - {{ printf "-tls-key-path=%s" $certificateSource.file.privateKeyFile | quote }}
            - {{ printf "-tls-ca-path=%s" $certificateSource.file.caCertificateFile | quote }}
            {{- end }}
            {{- end }}
            - -grpc-port={{ .Values.epe.service.grpcPort }}
            - -grpc-health-port={{ .Values.epe.service.healthPort }}
            - -metrics-port={{ .Values.epe.service.metricsPort }}
            - -audit-webhook-insecure-skip-verify={{ .Values.epe.auditWebhook.insecureSkipVerify }}
          env:
            - name: IDENTITY_PROVIDER_URL
              value: {{ .Values.epe.credentialProvider.url | quote }}
            - name: TOKEN_CACHE_TTL
              value: {{ .Values.epe.tokenCacheTTL | quote }}
            - name: TOKEN_CACHE_MAX_SIZE
              value: {{ .Values.epe.tokenCacheMaxSize | quote }}
            - name: CREDENTIAL_PROVIDER_MTLS_SOURCE
              value: {{ $source | quote }}
            {{- if eq $source "secret" }}
            - name: CREDENTIAL_PROVIDER_SECRET_NAMESPACE
              value: {{ .Values.epe.credentialProvider.mtls.secret.namespace | quote }}
            - name: CREDENTIAL_PROVIDER_SECRET_NAME
              value: {{ .Values.epe.credentialProvider.mtls.secret.name | quote }}
            {{- end }}
            {{- if .Values.epe.credentialProvider.mtls.insecureSkipVerify }}
            - name: CREDENTIAL_PROVIDER_INSECURE_SKIP_VERIFY
              value: "true"
            {{- end }}
            {{- range $name, $value := .Values.epe.env }}
            - name: {{ $name }}
              value: {{ $value | quote }}
            {{- end }}
          ports:
            - {name: extproc, containerPort: {{ .Values.epe.service.grpcPort }}}
            - {name: health, containerPort: {{ .Values.epe.service.healthPort }}}
            - {name: metrics, containerPort: {{ .Values.epe.service.metricsPort }}}
          livenessProbe:
            grpc: {port: {{ .Values.epe.service.healthPort }}, service: liveness}
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            grpc: {port: {{ .Values.epe.service.healthPort }}, service: readiness}
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            {{- toYaml .Values.epe.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: {drop: [ALL]}
            readOnlyRootFilesystem: true
            runAsUser: 65532
            runAsGroup: 65532
            runAsNonRoot: true
          {{- if or $mountRoot $mountToken .Values.epe.extraVolumeMounts }}
          volumeMounts:
            {{- if $mountRoot }}
            - name: workload-root
              mountPath: /var/run/secrets/agentio
              readOnly: true
            {{- end }}
            {{- if $mountToken }}
            - name: workload-token
              mountPath: /var/run/secrets/tokens
              readOnly: true
            {{- end }}
            {{- with .Values.epe.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- end }}
      {{- if or $mountRoot $mountToken .Values.epe.extraVolumes }}
      volumes:
        {{- if $mountRoot }}
        - name: workload-root
          configMap:
            name: {{ .Values.agentiod.ca.trustBundleConfigMapName }}
            items:
            - key: root-cert.pem
              path: root-cert.pem
        {{- end }}
        {{- if $mountToken }}
        - name: workload-token
          projected:
            sources:
            - serviceAccountToken:
                audience: {{ .Values.agentiod.tokenAudience | quote }}
                expirationSeconds: 3600
                path: agentio-token
        {{- end }}
        {{- with .Values.epe.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      {{- with .Values.epe.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.epe.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.epe.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.epe.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
